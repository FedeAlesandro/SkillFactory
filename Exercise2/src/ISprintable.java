public interface ISprintable {
    /**
     *
     * @param distance distance of the race (in km)
     * @return the time (hours) the race took to the competitor
     */
    public Double run(Double distance);
}
